{
  "name": "ppip-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite website",
    "build": "vite build website && node scripts/optimize-media.mjs --strict",
    "build:local": "vite build website && node scripts/optimize-media.mjs",
    "build:images": "vite build website && node scripts/optimize-media.mjs --skip-video"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}
